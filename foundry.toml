[profile.default]
src = "contracts/src"
out = "contracts/out"
libs = ["node_modules"]
allow_paths = ["."]
solc = "0.8.29"
evm_version = "cancun"

# Prefer building from contracts/ — see contracts/foundry.toml and contracts/deploy.md
