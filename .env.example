# Database
DATABASE_URL="postgresql://username:password@localhost:5432/writarcade"

# AI Services
OPENAI_API_KEY="sk-..."
ANTHROPIC_API_KEY="sk-ant-..."

# Content Scraping
SCRAPINGWEB_API_KEY="your-key"
EXTRACTOR_API_KEY="your-key"

# Authentication (for future)
NEXTAUTH_SECRET="your-secret"
NEXTAUTH_URL="http://localhost:3000"

# Base Network (Server-side only - DO NOT expose to client)
BASE_RPC_URL="https://mainnet.base.org"

# Smart Contracts (safe to expose - just addresses, no sensitive data)
NEXT_PUBLIC_WRITER_COIN_PAYMENT_ADDRESS="0x..." # WriterCoinPayment contract address
NEXT_PUBLIC_GAME_NFT_ADDRESS="0x..." # GameNFT contract address
NEXT_PUBLIC_GAME_NFT_CHAIN_ID="8453" # Base mainnet chain ID

# Neynar API (Server-side only - DO NOT expose API key to client)
NEYNAR_API_KEY="your-key"
# Note: Only expose NEXT_PUBLIC_NEYNAR_API_KEY if using their public endpoints (not recommended)
# Better: proxy through backend at /api/farcaster/profile

# Story Protocol Integration (IPFi Hackathon)
STORY_NETWORK="testnet" # "testnet" or "mainnet"
STORY_RPC_URL="https://testnet-rpc.story.foundation" # Override if using custom RPC
STORY_WALLET_KEY="0x..." # Private key for registering IP assets (server-side only)
STORY_IP_REGISTRATION_ENABLED="true" # Set to false to disable IP registration

# Story Contract Addresses (safe to expose - read-only)
NEXT_PUBLIC_STORY_IP_REGISTRY="0x..." # Story IP Asset Registry
NEXT_PUBLIC_STORY_ROYALTY_CONTRACT="0x..." # Story Royalty Token contract

# WritArcade Treasury on Story
NEXT_PUBLIC_WRITARCADE_TREASURY="0x..." # Platform's wallet for receiving 10% of royalties

# IPFS Gateway for storing game metadata
IPFS_GATEWAY="https://gateway.pinata.cloud" # Or your preferred IPFS gateway
PINATA_API_KEY="your-key" # For uploading game metadata to IPFS (optional)